# StockPulse

A stock tracker for your terminal

# Dev

First type `source .venv/bin/activate`
Then start everything by typing `python main.py`
If you only want to look at the tui, type `textual run --dev ui/app.py`
